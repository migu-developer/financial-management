/**
 * Shared constants for transactional emails (Cognito custom message templates).
 * Cognito replaces these placeholders when sending.
 */
export const COGNITO_CODE_PLACEHOLDER = '{####}';
export const COGNITO_USERNAME_PLACEHOLDER = '{username}';
