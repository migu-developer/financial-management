const passthrough = (str: string) => str;

const chalk = {
  blue: passthrough,
  green: passthrough,
  yellow: passthrough,
  red: passthrough,
  cyan: passthrough,
  gray: passthrough,
};

export default chalk;
