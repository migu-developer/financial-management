import { FilterChip } from '.';

describe('FilterChip', () => {
  it('exports a function component', () => {
    expect(typeof FilterChip).toBe('function');
  });

  it('has the expected name', () => {
    expect(FilterChip.name).toBe('FilterChip');
  });
});
