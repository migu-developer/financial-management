import { QuickFilterChips } from '.';

describe('QuickFilterChips', () => {
  it('exports a function component', () => {
    expect(typeof QuickFilterChips).toBe('function');
  });

  it('has the expected name', () => {
    expect(QuickFilterChips.name).toBe('QuickFilterChips');
  });
});
