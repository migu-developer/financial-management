import { FilterBarSkeleton } from '.';

describe('FilterBarSkeleton', () => {
  it('exports a function component', () => {
    expect(typeof FilterBarSkeleton).toBe('function');
  });

  it('has the expected name', () => {
    expect(FilterBarSkeleton.name).toBe('FilterBarSkeleton');
  });
});
